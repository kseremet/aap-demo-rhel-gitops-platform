---
# Generated by playbooks/setup/03_discover_inventory.yml.
# Management addresses are platform facts, not RHEL desired state.
all:
  children:
    rhel_fleet:
      vars:
        ansible_user: cloud-user
        ansible_become: true
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
      children:
        web_servers:
          hosts:
            web-01:
              ansible_host: 10.130.1.98
            web-02:
              ansible_host: 10.130.1.99
        database_servers:
          hosts:
            db-01:
              ansible_host: 10.130.1.101
        app_servers:
          hosts:
            app-01:
              ansible_host: 10.130.1.100
